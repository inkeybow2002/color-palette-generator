{
  "name": "color-palette-generator",
  "version": "1.0.0",
  "description": "An interactive web application that allows users to create and customize color palettes based on modern design principles.",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react": "^18.2.0", // Updated to the latest version
    "react-dom": "^18.2.0", // Updated to the latest version
    "react-scripts": "5.0.2" // Updated to the latest version
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}